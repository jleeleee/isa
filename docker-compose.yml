# vim: et:ts=2:sw=2

version: '3'

services:
  web:
    image: tp33/django
    network_mode: default
    external_links:
      - mysql:db
    volumes:
      - ./app:/app
    ports:
      - "8001:8000"
    command:
      # bash -c "/usr/bin/env python3 /app/manage.py runserver 0.0.0.0:8000"
      bash -c "python /app/manage.py makemigrations users listings reviews &&
               python /app/manage.py migrate &&
               mod_wsgi-express start-server --reload-on-changes --working-directory /app /app/partex/wsgi.py"
               #python /app/manage.py loaddata /app/partex/db/fixtures/users.json &&
               #python /app/manage.py loaddata /app/partex/db/fixtures/abstractitem.json &&
               #python /app/manage.py loaddata /app/partex/db/fixtures/listing.json &&
               #python /app/manage.py loaddata /app/partex/db/fixtures/review.json &&
